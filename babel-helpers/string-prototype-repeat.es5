module.exports = String.prototype.repeat || function(n) {
	if (
		(this!=null && this.repeat!==undefined) ||
		(typeof this !== 'string')
	) {
		return this.repeat(n)
	} else {
		return Array(n+1).join(this)
	}
}
